This folder is reserved for brand and illustration assets (SVGs, PNGs, etc.).

Import images from here into your React components using standard import syntax, for example:

```js
import heroIllustration from "../assets/hero-illustration.svg";
```

